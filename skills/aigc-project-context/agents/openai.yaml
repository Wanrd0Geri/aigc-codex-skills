interface:
  display_name: "AIGC Project Context"
  short_description: "Compile scripts and storyboards into shot context."
  default_prompt: "用 $aigc-project-context 从剧本和分镜整理镜头任务卡、连续性和表演理解。"
policy:
  allow_implicit_invocation: true
