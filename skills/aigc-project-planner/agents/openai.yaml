interface:
  display_name: "AIGC Project Planner"
  short_description: "Plan multi-stage AIGC projects and route specialist work."
  default_prompt: "用 $aigc-project-planner 帮我规划这个 AIGC 项目的整体流程，并判断下一步该用哪个 skill。"

policy:
  allow_implicit_invocation: true
