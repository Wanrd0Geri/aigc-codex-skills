interface:
  display_name: "AIGC Video"
  short_description: "默认先确认新镜头结构，明确授权直出；保持空间、动态与修复连续。"
  default_prompt: "用 $aigc-video 根据 brief、素材或成片写出、优化或回检视频提示词；默认先确认新结构，明确要求跳过时直接编译，并完整复核空间、动态与修改影响。"
policy:
  allow_implicit_invocation: true
