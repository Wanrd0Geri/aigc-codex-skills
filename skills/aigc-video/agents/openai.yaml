interface:
  display_name: "AIGC Video"
  short_description: "先确认每枚镜头结构，再生成或重编完整、连续的视频提示词。"
  default_prompt: "用 $aigc-video 根据 brief、素材或成片写出、优化或回检视频提示词。所有新镜头先确认结构再补表演；修改时完整复核影响范围，并至少返回一枚完整受影响镜头或完整操作命令。"
policy:
  allow_implicit_invocation: true
