# Observed Seedance 2.5 Optimizer Example

This is one user-supplied output from 即梦官网提示词优化助手, received 2026-08-03. Use it only when comparing format choices, reviewing a rewrite, maintaining this Skill, or updating regression cases. It is evidence of one optimizer pattern, not a universal mandatory grammar. Do not copy its characters, plot, sound, shot count, or field presence into unrelated prompts.

Reusable observations:

- plain colon headings rather than bracketed control headings
- integer timestamp ranges in `镜头N（开始-结束秒）：`
- ordinary quotation marks for dialogue rather than brace or bracket control markers
- persistent ambience in `场景：` and local sound inside the relevant shot
- compact natural sentences and no separate sound section
- absence of `全局补充：` in this single observed output is not evidence against the standing personal default `无字幕、无音乐`

The full supplied output is intentionally not retained in the runtime reference. The observations above and the corresponding regression cases preserve the only reusable format evidence without carrying unrelated plot content into future prompts.
