interface:
  display_name: "AIGC Natural Language Prompt"
  short_description: "Rewrite AIGC prompts into director-style natural language."
  default_prompt: "Use $aigc-natural-language-prompt to rewrite this prompt into director-style natural-language visual instructions."

policy:
  allow_implicit_invocation: true
