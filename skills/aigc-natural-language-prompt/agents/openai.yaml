interface:
  display_name: "AIGC Natural Language Prompt"
  short_description: "Clean AIGC prompt language, logic, and AI-flavored phrasing."
  default_prompt: "Use $aigc-natural-language-prompt to remove AI-flavored phrasing, ambiguity, and parameter stacking from this AIGC prompt."

policy:
  allow_implicit_invocation: true
