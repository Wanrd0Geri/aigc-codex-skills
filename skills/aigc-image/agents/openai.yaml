interface:
  display_name: "AIGC Image Editor"
  short_description: "Diagnose, edit, or language-repair image-edit prompts."
  default_prompt: "用 $aigc-image 分析我上传的图片，并调用最少的编辑能力生成可直接使用的修图提示词。"
policy:
  allow_implicit_invocation: true
