interface:
  display_name: "AIGC Image"
  short_description: "Create, diagnose, reconstruct, and edit image prompts."
  default_prompt: "用 $aigc-image 根据我的文字 brief 或图片，直接生成、反推或编写可使用的修图提示词。"
policy:
  allow_implicit_invocation: true
