interface:
  display_name: "AIGC Image"
  short_description: "Diagnose, reconstruct, and edit AIGC images."
  default_prompt: "用 $aigc-image 诊断这张图，并根据我的目标直接给出反推或修图提示词。"
policy:
  allow_implicit_invocation: true
