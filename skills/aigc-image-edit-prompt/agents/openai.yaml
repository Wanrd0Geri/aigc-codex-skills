interface:
  display_name: "AIGC Image Edit Prompt"
  short_description: "Write precise image-edit prompts for AIGC keyframes."
  default_prompt: "用 $aigc-image-edit-prompt 诊断这张关键帧，并写一份适合图像编辑器的中英双语修图 prompt，重点控制光线、色彩、景深和主体保留。"

policy:
  allow_implicit_invocation: true
