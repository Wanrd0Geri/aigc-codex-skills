# 2026-09-05 图像与视频 Skill 结构修订证据索引

本次保留并修订当前 `aigc-image` / `aigc-video`，没有恢复退休旧入口。原始基线是 `b8a8afe7d471944995799cbeb432a92bc6466b61`。此文件只记录可追踪证据和边界，不为 Skill 生成通用质量分。

完整运行档案作为用户侧伴随交付物 `AIGC-Skill-执行证据.tar.xz` 保存，体积较大的运行数据不进入日常 Skill 上下文。以下路径从该档案的 `skill-structure-runs/` 起算。实际媒体使用对应文件字节与 SHA-256；UI 未暴露的模型子版本、温度、seed 均未推断。

| 范围 | 证据及位置 | 能证明的范围 |
| --- | --- | --- |
| 固定对照 | `forward/condition-k` = 原始基线；`condition-r` = 初版候选；`condition-s` = 相机修复；`condition-t` = 最终目标连带修复。对应 baseline/candidate/candidate-s/candidate-t-hashes.json | 可识别规则快照；不是生成质量 |
| 9个单轮请求 × 3条件 | `forward/single-requests.json`、`results-k/m/r`、`archives-final`，m为无Skill；原始输出与实际读取清单完整保留 | 27条实际回复；每条件在自己的会话内批量处理，不是假设完全独立采样 |
| 4轮实际顺序交互 × 3条件 | `results-k/m/r/revision-t1…t4`、`archives-sequential` | 12条实际回复；后续用户输入在前轮完成后发送。原始状态JSON是assistant_reported，不是工具执行证明 |
| 相机修复后续答和成对请求 | `retest-requests.json`、`results-s`、`archives-retests/s`、`blind/retest-camera-review.*` | 3条实际回复经独立语义评审通过；含给定原前三轮历史的第4轮续答，未冒充重新执行4轮 |
| 目标支持范围与相机边界追加检查 | `retest-final-requests.json`、`results-t`、`archives-retests/t`、`blind/retest-final-review.*` | 5条实际回复，当轮语义处理均通过；含同源改色重放、独立2D形状、明确超现实反射、硬约束冲突及相机动作锁。内部“已查看”等自述不作为执行证明 |
| 首轮独立匿名审查 | `blind/review.*`、`blind/revision-review.*`、`blind/strict-edit-addendum.*` | 初版候选确实出现只改机位却新增转头、灰布改色未覆盖可见倒影两项失败；全部原件保留 |
| 真实图像实验 | `actual-image-run-manifest.json`、`media/`、`prompts/`、`forward/archives-media/` | 4张实际编辑图，每条件每任务1次；原始brief直接生图是另一个对照，不冒充无Skill编译器结果 |
| 实际视频服务尝试 | `video-provider-attempts.json` | 两次调用均在生成前 INVALID_ARGUMENT，无creation ID或成片；没有音视频输出可验证 |
| 检查器独立复核 | `implementation-check.md`、`implementation-check-closure.md`、对应results.json | 初始5项和随后3项边界反例完成闭环。只验证检查器，不计模型forward |

单轮初始盲审为24 pass、3 uncertain；补足真实源视频后，候选严格编辑的1项uncertain改判fail，形成24 pass、2 uncertain、1 fail。两项uncertain都是保留字面锚点但原文未发生语言改善，不以强行改写换取通过。无Skill对照在多轮案例中通过；不能据本次样本声称新版整体胜过旧版或无Skill。

图像细节：候选新增灯具样本的可见台灯、暖光、光晕及反射通过；原始brief灯具的反射完整性不确定。两张删杯结果都清除了杯体与倒影，但改变了受保护墙面纹理，严格保留失败。未授予通用 `output-verified` 或跨平台可靠性状态。

维护检查：图像30条、视频262条文本用例是验收规格；15个eval案例/13个族与6组11轮手写状态示例不是模型执行记录。39项Python自测、Skill入口校验、文件引用与冻结素材哈希检查通过。5份必需素材仍明确待提供：flare源图、小污点标记源、暖棚角色、月光庭院、已校验十镜上下文。不得把它们算作已测。

最终harness SHA-256为 `c856574e6f9d9351dce5b19bed62af25c2a3accb94323b8602e75e6ab549ea16`。离线重放使用归档内case/submission/rules/assets和实际字节，不要求原始绝对素材路径仍然存在；不同版本的harness原件保存在 `forward/harness-versions/`。确定性结果、独立模型语义评审和真实媒体评审各自保留，不合成一个总分。
